[package]
name = "odin-bench"
version = "1.0.0"
edition = "2021"
publish = false

[dependencies]
odin-core = { path = "../../../sdk/rust/odin-core", features = ["crypto", "regex"] }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
